name: qs_navigation
description: QS Navigation
version: 0.0.6
homepage: 'https://github.com/artsiombarouski/qs_navigation.git'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  url_strategy: ^0.3.0
  path_to_regexp: ^0.4.0
  universal_platform: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
